Completion state inside the members area (module progress, Arquitetura ARCO™).

```jsx
<ProgressBar label="Pilar C — Construção da Decisão" value={60} />
```

Gold gradient fill on a sand track. Use `size="sm"` inside list rows, `md` in headers.
