FAQ and curriculum lists. Hairline rules between rows, Cinzel question, gold `+` that rotates to `×`. One open at a time.

```jsx
<Accordion items={[{question:'Serve para quem nunca vendeu high ticket?',answer:'Sim — o ARCO começa na Análise da Lacuna.'}]} />
```

Answers stay under ~320px tall; longer content belongs on its own page.
