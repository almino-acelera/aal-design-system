Multi-select and consent control. Square, 18px, olive fill when checked.

```jsx
<Checkbox label="Aceito receber os avisos da turma no WhatsApp" checked={ok} onChange={e => setOk(e.target.checked)} />
```

`description` adds a second line in caption grey for legal or clarifying copy.
