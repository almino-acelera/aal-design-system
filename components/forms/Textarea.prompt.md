Multi-line field. Same label and focus treatment as `Input`; used in application forms where qualification answers matter.

```jsx
<Textarea label="O que travou sua última negociação?" rows={5} placeholder="Escreva em duas ou três frases." />
```
