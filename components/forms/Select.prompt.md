Dropdown for qualification questions with a fixed answer set.

```jsx
<Select label="Ticket médio" options={["Até R$ 2 mil", "R$ 2 a 10 mil", "Acima de R$ 10 mil"]} />
```

Metrics match `Input` so they align in a grid. The chevron is a CSS caret in gold — don't swap in an icon font.
