Single-choice control for short mutually exclusive answers (2–4 options). Beyond that use `Select`.

```jsx
<Radio name="area" label="Vendas internas (inside sales)" checked={v==='inside'} onChange={()=>setV('inside')} />
```
