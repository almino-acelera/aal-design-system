Pill filter / topic label. This is the only place the system uses a pill radius — buttons and cards stay squared.

```jsx
<Tag active onClick={() => {}}>Todos</Tag>
<Tag onClick={() => {}}>Objeções</Tag>
<Tag onRemove={() => {}}>Fechamento</Tag>
```

Sentence-case labels (not uppercase — that's `Badge`). `active` fills olive.
