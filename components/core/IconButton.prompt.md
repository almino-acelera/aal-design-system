Square icon-only button for toolbars, media controls, dialog close and carousel arrows.

```jsx
<IconButton label="Fechar" variant="ghost"><i data-lucide="x"></i></IconButton>
```

Always pass `label`. Variants `outline | solid | ghost | onDark`; sizes 32/40/48px. Use `onDark` inside olive sections and over imagery.
